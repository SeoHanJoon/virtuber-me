# VirtualBer-Me 발표 스크립트 (20분)

## 📋 타임라인

- **0-2분**: 인트로 & 데모
- **2-7분**: 프로젝트 개요 & 기술 스택
- **7-12분**: 핵심 구조 & 데이터 흐름
- **12-17분**: 기술적 도전과 해결
- **17-20분**: 성과 & Q&A

---

## 🎬 0-2분: 인트로 & 라이브 데모

### [화면: 두 개의 브라우저 창 준비]

**"안녕하세요. 오늘 제가 소개할 프로젝트는 'VirtualBer-Me', 웹 기반 실시간 멀티플레이어 VRM 아바타 월드입니다."**

**"먼저 간단하게 동작하는 모습을 보여드리겠습니다."**

### [데모 시작]

1. **첫 번째 창**: 룸 접속, 닉네임 "발표자" 입력
2. **두 번째 창**: 같은 룸 접속, 닉네임 "참여자" 입력

**"보시는 것처럼 두 사용자가 같은 가상 공간에 3D 아바타로 들어왔습니다."**

3. **첫 번째 창**: WASD로 이동

**"WASD 키로 이동할 수 있고,"**

4. **두 번째 창**: 상대방 아바타가 움직이는 것 확인

**"다른 사용자의 화면에서 제 움직임이 실시간으로 반영됩니다."**

5. **웹캠 앞에서 표정 변화**

**"그리고 제 얼굴 표정도 자동으로 아바타에 반영되죠."**

**"이 모든 것이 웹 브라우저에서, 별도 설치 없이 작동합니다."**

---

## 📊 2-7분: 프로젝트 개요 & 기술 스택

### [화면: PRESENTATION.md - 프로젝트 개요 섹션]

**"이 프로젝트의 핵심 기능은 크게 세 가지입니다."**

**"첫째, MediaPipe Face Landmarker를 사용한 실시간 얼굴 표정 추적입니다. 웹캠에서 468개의 얼굴 랜드마크를 감지하고, 이를 VRM 아바타의 표정으로 변환합니다."**

**"둘째, WASD 키보드 제어입니다. 게임처럼 직관적으로 아바타를 움직일 수 있습니다. 여기서 한 가지 재미있는 점은, 한글 입력 모드에서도 작동한다는 겁니다. 이건 나중에 자세히 설명드리겠습니다."**

**"셋째, Socket.IO를 통한 실시간 멀티플레이어 동기화입니다. 최대 100명까지 한 공간에서 소통할 수 있도록 설계했습니다."**

### [화면: 기술 스택 섹션]

**"기술 스택을 보면, 프론트엔드는 Next.js 15와 TypeScript를 기반으로 합니다."**

**"3D 렌더링은 Three.js를 사용하고, VRM 모델 로딩을 위해 Pixiv에서 만든 three-vrm 라이브러리를 사용했습니다."**

**"VRM이란 일본의 VirtualCast에서 만든 3D 아바타 표준 포맷인데, glTF 기반으로 표정, 본 구조, 물리 엔진까지 포함하고 있어서 아바타 제작에 많이 사용됩니다."**

**"백엔드는 Node.js에 Socket.IO 서버로 구성했습니다. 실시간 양방향 통신이 필요했기 때문에 WebSocket 기반 솔루션을 선택했습니다."**

---

## 🏗️ 7-12분: 시스템 아키텍처 & 데이터 흐름

### [화면: 시스템 아키텍처 다이어그램]

**"전체 시스템 구조를 보겠습니다."**

**"클라이언트는 크게 세 부분으로 나뉩니다."**

**"먼저 Face Tracker가 웹캠에서 얼굴을 추적하고, Controls가 키보드 입력을 처리합니다. 이 두 입력이 MultiplayerVRMWorld 컴포넌트로 들어가서 Three.js로 렌더링됩니다."**

**"NetworkSync 레이어는 Socket.IO를 통해 서버와 통신합니다."**

**"서버는 Room Manager를 중심으로 동작합니다. 각 룸은 독립적으로 관리되며, 한 룸에서 발생한 업데이트는 같은 룸의 사용자들에게만 브로드캐스트됩니다."**

### [화면: 데이터 구조 섹션]

**"핵심 데이터 구조를 보겠습니다."**

**"UserState는 한 사용자의 모든 상태를 담고 있습니다. 위치, 회전, 표정, 그리고 사용하는 VRM 모델 경로까지."**

**"네트워크로 전송할 때는 UpdatePayload로 변환합니다. Object 대신 Array를 사용해서 데이터 크기를 줄였습니다. 예를 들어 position을 {x, y, z} 대신 [x, y, z]로 보냅니다."**

**"이게 작은 최적화처럼 보이지만, 초당 10번씩 전송되니까 대역폭 절감 효과가 큽니다."**

### [화면: 데이터 흐름 섹션]

**"데이터 흐름을 보면, 클라이언트에서 서버로는 10Hz, 즉 100ms마다 한 번씩 업데이트를 보냅니다."**

**"더 자주 보낼 수도 있지만, 네트워크 부하와 실시간성의 균형을 고려해서 10Hz로 설정했습니다. 실제로 사용해보면 충분히 부드럽습니다."**

**"서버는 받은 업데이트를 같은 룸의 다른 사용자들에게 브로드캐스트하고, 클라이언트는 받은 데이터로 다른 사용자의 VRM 위치와 회전을 업데이트합니다."**

**"이때 보간(interpolation)을 사용합니다. 위치는 lerp, 회전은 slerp를 써서 뚝뚝 끊기지 않고 부드럽게 움직이도록 했습니다."**

---

## 💪 12-17분: 기술적 도전과 해결

### [화면: 기술적 도전과 해결 섹션]

**"개발하면서 재미있는 문제들을 많이 만났는데, 그 중 다섯 가지를 소개하겠습니다."**

### 1. 클로저 문제

**"첫 번째, React의 클로저 문제입니다."**

**"WASD로 이동하려고 하는데, 키를 눌러도 아바타가 움직이지 않는 거예요."**

**"원인을 찾아보니, Three.js의 애니메이션 루프가 requestAnimationFrame으로 계속 실행되는데, 이 루프 안에서 참조하는 state가 초기값으로 고정되어 있었습니다. React의 클로저 특성 때문이죠."**

**"해결은 useRef를 사용했습니다. state가 바뀔 때마다 ref를 업데이트하고, 애니메이션 루프에서는 ref.current를 참조하도록 했습니다."**

```typescript
const controlsRef = useRef(controls);
useEffect(() => {
  controlsRef.current = controls;
}, [controls]);

// 애니메이션 루프
const currentControls = controlsRef.current;
```

**"이 패턴은 network와 expression에도 적용했습니다."**

### 2. 한영 키 입력 문제

**"두 번째는 한영 키 문제입니다."**

**"처음에는 e.key로 키 입력을 감지했어요. 'w', 'a', 's', 'd' 이런 식으로요. 그런데 한글 모드로 바꾸면 갑자기 작동을 안 하는 겁니다."**

**"알고 보니 한글 모드에서는 W키를 누르면 e.key가 'ㅈ'이 되더라고요."**

**"해결은 간단했습니다. e.key 대신 e.code를 쓰면 됩니다. e.code는 키보드의 물리적 위치를 반환하거든요. W키는 항상 'KeyW'입니다, 한글이든 영문이든."**

```typescript
switch (e.code) {
  case 'KeyW':
    next.forward = true;
    break;
  case 'KeyA':
    next.left = true;
    break;
  case 'KeyS':
    next.backward = true;
    break;
  case 'KeyD':
    next.right = true;
    break;
}
```

**"이게 특히 한국 사용자들에게는 중요한 개선이었습니다."**

### 3. VRM 잔상 문제

**"세 번째는 VRM 잔상 문제입니다."**

**"다른 사용자가 움직이는데, 원래 있던 위치에 아바타가 그대로 남아있고, 새 위치에도 아바타가 나타나는 거예요. 일종의 잔상이죠."**

**"원인은 VRM이 중복으로 로드되고 있었습니다. 애니메이션 루프와 useEffect, 두 곳에서 동시에 로드를 시도하고 있었어요."**

**"해결은 세 가지를 했습니다."**

**"첫째, loadingVRMs라는 Set을 만들어서 현재 로딩 중인 VRM을 추적합니다. 이미 로딩 중이면 중복 로드를 막습니다."**

**"둘째, VRM 로드는 useEffect에서만 하고, 애니메이션 루프는 이미 로드된 VRM만 업데이트하도록 역할을 분리했습니다."**

**"셋째, 렌더러에 autoClear를 활성화하고 명시적으로 clear()를 호출해서 프레임마다 깨끗하게 지우도록 했습니다."**

### 4. 중복 아바타 표시

**"네 번째는 자신의 아바타가 두 개 보이는 문제입니다."**

**"접속하기 버튼을 누르면 자기 아바타가 두 개가 되는 거예요. 하나는 내가 조종하는 아바타, 하나는 다른 사용자로 표시되는 아바타."**

**"이것도 클로저 문제였습니다. 서버에서 user_update 이벤트가 오면, myUserId와 비교해서 자신의 업데이트는 무시해야 하는데, 이 비교가 제대로 안 되고 있었어요."**

**"useState의 setter 함수 안에서 또 다른 setState를 호출할 때, 외부 변수를 참조하면 클로저로 묶여서 초기값을 보게 됩니다."**

**"해결은 함수형 업데이트를 사용했습니다."**

```typescript
setMyUserId((currentMyId) => {
  setOtherUsers((prev) => {
    if (user.id !== currentMyId) {
      // currentMyId는 최신값
    }
  });
  return currentMyId;
});
```

**"이렇게 하면 항상 최신 myUserId를 참조할 수 있습니다."**

### 5. 무한 리렌더링

**"마지막은 무한 리렌더링입니다."**

**"참여자 목록을 표시하는데 갑자기 브라우저가 멈춰버렸어요. 콘솔을 보니 'Maximum update depth exceeded' 에러가 나더라고요."**

**"원인은 렌더링할 때마다 새로운 Map 객체를 생성해서 props로 전달하고 있었습니다. React는 props가 바뀌면 리렌더링하니까 무한 루프에 빠진 거죠."**

**"해결은 useMemo입니다."**

```typescript
const allUsers = useMemo(() => {
  const all = new Map(network.otherUsers);
  // ... 객체 생성
  return all;
}, [의존성배열]);
```

**"의존성이 실제로 변경됐을 때만 새 객체를 만들도록 메모이제이션했습니다."**

---

## 🏆 17-20분: 성과, 향후 계획 & Q&A

### [화면: 핵심 성과 섹션]

**"지금까지의 개발 결과를 정리하면,"**

**"첫째, 100ms 이하의 레이턴시로 실시간 동기화를 구현했습니다. 최대 100명까지 지원 가능하도록 설계했고요."**

**"둘째, 완전히 웹 기반입니다. 사용자는 링크만 클릭하면 바로 접속할 수 있습니다."**

**"셋째, 멀티룸 구조로 확장성을 확보했습니다. 각 룸이 독립적이기 때문에 수평 확장이 쉽습니다."**

### [화면: 향후 계획 섹션]

**"향후 계획은 단기적으로 음성 채팅을 추가하는 겁니다. WebRTC로 구현할 예정입니다."**

**"중기적으로는 텍스트 채팅, 커스텀 VRM 업로드, 그리고 사용자 프로필 저장 기능을 넣을 생각입니다."**

**"장기적으로는 상호작용 가능한 오브젝트, 커스텀 맵, 그리고 모바일 지원까지 고려하고 있습니다."**

**"그리고 처음에 시도했던 상체 트래킹도, 기술이 안정화되면 다시 도입해볼 생각입니다."**

### 마무리

**"이 프로젝트를 통해 배운 것들이 많습니다."**

**"React와 Three.js처럼 서로 다른 패러다임의 라이브러리를 통합하는 방법, 실시간 동기화 시스템 설계, 그리고 웹 기반 3D 애플리케이션의 성능 최적화 등등."**

**"특히 클로저 문제를 해결하면서 JavaScript와 React의 작동 원리를 깊이 이해하게 됐습니다."**

**"발표 내용이 담긴 문서는 프로젝트 레포에 PRESENTATION.md로 올려뒀으니 참고하시면 됩니다."**

**"궁금하신 점 있으면 질문 주세요. 감사합니다!"**

---

## 🎯 예상 질문 & 답변

### Q1: "VRChat이나 다른 기존 플랫폼과의 차이점은?"

**A:** "좋은 질문입니다. VRChat은 Unity 기반으로 별도 설치가 필요하고, VR 장비가 있어야 제대로 즐길 수 있습니다. 반면 이 프로젝트는 완전히 웹 기반이라 링크만 보내면 누구나 바로 접속할 수 있습니다. 또한 교육 목적으로 만들었기 때문에 코드가 모두 공개되어 있어서 학습 자료로도 활용할 수 있습니다."

### Q2: "확장성은 어떻게 확보하나요?"

**A:** "멀티룸 구조가 핵심입니다. 각 룸이 독립적이기 때문에 서버를 여러 대로 늘리고 로드밸런서로 분산할 수 있습니다. 예를 들어 룸 ID를 해싱해서 특정 서버로 라우팅하는 식으로요. 또한 Redis 같은 캐시를 추가하면 서버 간 상태 공유도 가능합니다."

### Q3: "보안은 어떻게 처리하나요?"

**A:** "현재는 프로토타입이라 인증 시스템이 없습니다. 향후에는 JWT 기반 인증, 룸 비밀번호, 그리고 권한 관리 시스템을 추가할 계획입니다. 또한 Rate Limiting을 강화해서 DDoS 공격을 방어하고, 입력값 검증을 추가해서 XSS나 injection 공격을 막을 겁니다."

### Q4: "왜 WebSocket을 선택했나요? WebRTC는?"

**A:** "WebRTC도 고려했습니다. P2P로 직접 연결되니까 레이턴시가 낮죠. 하지만 몇 가지 이유로 WebSocket을 선택했습니다. 첫째, 중앙 서버가 필요했습니다. 룸 관리, 권한 제어, 로깅 같은 기능들 때문이죠. 둘째, 1:N 브로드캐스트가 효율적입니다. WebRTC는 각 피어마다 연결을 맺어야 하는데, 사용자가 많아지면 복잡도가 N²로 증가합니다. 셋째, WebRTC는 NAT나 방화벽 때문에 연결 실패가 많은데, WebSocket은 상대적으로 안정적입니다."

### Q5: "성능 병목은 어디인가요?"

**A:** "현재는 클라이언트 렌더링이 병목입니다. Three.js가 모든 VRM을 매 프레임 렌더링하니까요. 해결 방법은 몇 가지 있습니다. 첫째, 거리 기반 컬링을 이미 적용했고, 둘째는 LOD(Level of Detail)를 추가해서 먼 거리의 아바타는 낮은 품질로 렌더링할 수 있습니다. 셋째는 Frustum Culling으로 카메라 시야 밖의 오브젝트는 렌더링하지 않도록 할 수 있고요. 서버는 아직 여유가 많습니다."

### Q6: "MediaPipe를 선택한 이유는?"

**A:** "MediaPipe는 Google에서 만든 라이브러리로, 웹 브라우저에서 바로 실행됩니다. WASM으로 컴파일되어 있어서 성능도 좋고, 사전 훈련된 모델을 제공해서 바로 사용할 수 있습니다. 다른 대안으로 TensorFlow.js도 있었는데, MediaPipe가 더 가볍고 특화되어 있어서 선택했습니다."

### Q7: "실제 서비스로 출시할 계획은?"

**A:** "현재는 학습 프로젝트이지만, 기능이 더 안정화되면 베타 서비스로 열어볼 생각입니다. 특히 온라인 수업이나 소규모 모임 같은 특정 유스케이스에 맞춰서 개선할 수 있을 것 같습니다. 비용은 서버와 대역폭이 주된 부분일 텐데, 프리티어로 시작해서 사용량에 따라 스케일업하는 방식을 고려하고 있습니다."

---

## 📝 발표 전 체크리스트

### 기술적 준비

- [ ] 서버 실행 확인 (localhost:3001)
- [ ] 클라이언트 실행 확인 (localhost:3000)
- [ ] 두 개의 브라우저 창 준비
- [ ] 웹캠 권한 허용
- [ ] 네트워크 연결 확인
- [ ] VRM 모델 로드 테스트

### 자료 준비

- [ ] PRESENTATION.md 열어두기
- [ ] VS Code에서 주요 파일 열어두기:
  - MultiplayerVRMWorld.tsx
  - useControls.ts
  - useNetworkSync.ts
  - server/index.ts
- [ ] 아키텍처 다이어그램 확대
- [ ] 콘솔 개발자 도구 열어두기

### 연습

- [ ] 전체 스크립트 1회 리허설
- [ ] 데모 시나리오 연습
- [ ] 타이밍 체크 (20분 엄수)
- [ ] 예상 질문 답변 준비

### 발표 팁

- ✅ 자신감 있게, 천천히 말하기
- ✅ 청중과 아이컨택 유지
- ✅ 기술 용어는 간단히 설명하기
- ✅ 데모 중 문제 발생 시 당황하지 않기 (예비 영상 준비)
- ✅ 시간 관리 (17분까지 마무리, 3분은 Q&A)

---

## 🎤 발표 시작 전 한마디

**"숨 크게 쉬고, 여유롭게 시작하세요. 당신이 만든 프로젝트는 충분히 멋집니다. 기술적인 디테일보다는 '왜 이걸 만들었는지', '어떤 문제를 해결했는지'에 집중하세요. 청중은 당신의 열정과 문제 해결 능력을 보고 싶어합니다. 화이팅!"**
