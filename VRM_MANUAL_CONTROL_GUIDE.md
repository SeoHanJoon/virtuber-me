# VRM 수동 제어 가이드

## 🎮 개요

VRM 모델의 각 본(Bone) 회전을 슬라이더로 수동 제어할 수 있는 기능입니다. 자동 추적과 별개로 직접 값을 조정하여 원하는 포즈를 만들 수 있습니다.

## 📍 위치

- **경로**: `/face-tracking` 페이지
- **UI 위치**: 화면 우측 상단 (설정 패널 위)

## 🔧 주요 기능

### 1️⃣ 제어 가능한 본(Bone)

| 본 이름          | 회전 축 | 설명                                      |
| ---------------- | ------- | ----------------------------------------- |
| **척추 (Spine)** | X, Y, Z | 상체 기울임 (앞/뒤, 좌/우, 회전)          |
| **가슴 (Chest)** | X, Y, Z | 상체 상단 기울임                          |
| **왼팔 상완**    | X, Y, Z | 왼쪽 어깨~팔꿈치 회전                     |
| **왼팔 전완**    | X, Y, Z | 왼쪽 팔꿈치~손목 회전                     |
| **오른팔 상완**  | X, Y, Z | 오른쪽 어깨~팔꿈치 회전                   |
| **오른팔 전완**  | X, Y, Z | 오른쪽 팔꿈치~손목 회전                   |
| **머리 (Head)**  | X, Y, Z | 머리 회전 (고개 숙임/들기, 좌/우, 갸우뚱) |

### 2️⃣ 회전 축 설명

- **X (Pitch)**: 앞/뒤 기울임 (고개 숙이기/들기)
- **Y (Yaw)**: 좌/우 회전 (고개 돌리기)
- **Z (Roll)**: 좌/우 기울임 (갸우뚱하기)

### 3️⃣ 값 범위

- **범위**: `-π ~ +π` (라디안)
- **표시**: 도(°) 단위로 표시 (`-180° ~ +180°`)
- **실시간**: 슬라이더 옆에 현재 값이 실시간으로 표시됨

## 📖 사용 방법

### 1. 수동 제어 활성화

```
1. /face-tracking 페이지 접속
2. 우측 상단 "🎮 수동 제어 OFF" 버튼 클릭
3. 버튼이 "🎮 수동 제어 ON" (파란색)으로 변경
```

### 2. 슬라이더 패널 열기

```
1. "▶" 버튼 클릭 → "▼"로 변경되며 패널 확장
2. 각 본의 슬라이더 조정
3. 실시간으로 VRM 모델에 반영됨
```

### 3. 값 조정

```
- 슬라이더를 좌우로 드래그
- 현재 값이 도(°) 단위로 표시됨
- 예: 45° = 0.785 라디안
```

### 4. 리셋

```
"리셋" 버튼 클릭 → 모든 회전값이 0으로 초기화
```

## ⚠️ 주의사항

### 🚫 자동 추적 비활성화

수동 제어가 **ON**일 때:

- ❌ 얼굴 추적 비활성화
- ❌ 상체 추적 비활성화
- ❌ 손 추적 비활성화

**이유**: 자동 추적과 수동 제어가 동시에 작동하면 충돌이 발생하기 때문

### 💡 권장 워크플로우

1. **자동 추적 먼저 테스트**
   - 수동 제어 OFF 상태에서 카메라로 추적
   - 대략적인 움직임 확인

2. **수동 제어로 미세 조정**
   - 자동 추적이 잘 안되는 부분을 수동으로 조정
   - 특정 포즈를 정확하게 만들 때 유용

3. **번갈아 사용**
   - 수동 제어 ON/OFF를 전환하며 사용

## 🎨 UI 설명

### 토글 버튼

```
🎮 수동 제어 OFF  (회색) → 클릭 → 🎮 수동 제어 ON (파란색)
```

### 슬라이더

```
척추 (Spine)
  X (Pitch)    [========|--------]  45°
  Y (Yaw)      [--------|--------]   0°
  Z (Roll)     [--------|--------]   0°
```

### 리셋 버튼

```
[리셋]  (빨간색) → 모든 값을 0으로 초기화
```

## 🔍 디버깅

### 값이 적용되지 않는 경우

1. **VRM 로드 확인**

   ```
   좌측 상단 상태: "VRM 로드 완료 ✓"
   ```

2. **수동 제어 활성화 확인**

   ```
   버튼 색상: 파란색 (ON)
   ```

3. **자동 추적 비활성화 확인**
   ```
   하단 메시지: "💡 수동 제어 ON 시 자동 트래킹이 비활성화됩니다"
   ```

### 예상치 못한 움직임

- VRM 모델마다 본 구조가 다를 수 있음
- 값을 작게 조정하며 테스트 권장 (예: ±30° 범위)
- 리셋 후 다시 시도

## 🚀 고급 활용

### 포즈 프리셋 만들기

1. 원하는 포즈를 슬라이더로 조정
2. 각 값(도)를 메모
3. 나중에 동일한 값으로 재현

### 애니메이션 키프레임

1. 포즈 A: 슬라이더 조정 → 스크린샷
2. 포즈 B: 슬라이더 조정 → 스크린샷
3. 두 포즈 사이를 보간하여 애니메이션 제작 가능

## 📊 값 참고표

### 일반적인 포즈 값 (도)

| 포즈               | 본        | X    | Y   | Z    | 설명          |
| ------------------ | --------- | ---- | --- | ---- | ------------- |
| **기본 자세**      | 척추      | 0°   | 0°  | 0°   | 똑바로 서기   |
| **앞으로 숙임**    | 척추      | 30°  | 0°  | 0°   | 상체 앞으로   |
| **좌측 기울임**    | 척추      | 0°   | 0°  | -20° | 상체 왼쪽으로 |
| **팔 들기 (왼)**   | 왼팔 상완 | 0°   | 0°  | -90° | 왼팔 옆으로   |
| **팔 앞으로 (왼)** | 왼팔 상완 | -90° | 0°  | 0°   | 왼팔 앞으로   |
| **팔 굽히기 (왼)** | 왼팔 전완 | 0°   | 0°  | 90°  | 팔꿈치 굽힘   |
| **고개 숙임**      | 머리      | 30°  | 0°  | 0°   | 머리 앞으로   |
| **고개 돌림 (좌)** | 머리      | 0°   | 45° | 0°   | 머리 왼쪽으로 |

## 🛠 기술 세부사항

### 내부 구조

```typescript
// 회전값 인터페이스
interface VRMManualRotation {
  spineX: number; // 척추 X축 (라디안)
  spineY: number; // 척추 Y축
  spineZ: number; // 척추 Z축
  // ... 기타 본들
}

// 적용 방식
spine.rotation.x = rotation.spineX;
spine.rotation.y = rotation.spineY;
spine.rotation.z = rotation.spineZ;
```

### 자동 추적 비활성화 로직

```typescript
// 얼굴 추적 (수동 제어 비활성화 시에만)
if (
  faceLandmarkerRef.current &&
  videoRef.current &&
  !manualControlEnabled  // ← 이 조건 추가
) {
  applyFaceTrackingToVRM(...);
}
```

## 📝 FAQ

### Q1. 슬라이더를 움직였는데 모델이 안 움직입니다.

A1. 수동 제어가 ON인지 확인하세요. 버튼이 파란색이어야 합니다.

### Q2. 값을 0으로 했는데도 모델이 기울어져 있습니다.

A2. VRM 모델의 기본 포즈가 다를 수 있습니다. "리셋" 버튼을 눌러보세요.

### Q3. 자동 추적과 동시에 사용할 수 없나요?

A3. 기술적으로 불가능합니다. 두 시스템이 같은 본을 동시에 제어하면 충돌이 발생합니다.

### Q4. 다른 페이지에서도 사용할 수 있나요?

A4. 현재는 `/face-tracking` 페이지에만 통합되어 있습니다. 필요시 다른 페이지에도 추가 가능합니다.

## 🔗 관련 파일

- **컴포넌트**: `components/VRMManualControl.tsx`
- **적용 로직**: `components/FaceTrackingVRMViewer.tsx`
- **타입 정의**: `VRMManualRotation` 인터페이스

---

**📌 팁**: 수동 제어로 원하는 포즈를 만든 후, 자동 추적으로 전환하면 그 포즈를 기준으로 움직임이 추가됩니다!
